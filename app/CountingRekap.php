<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class CountingRekap extends Model
{
    protected $table = "counting_rekap";
    //protected $fillable = ['vehicle', 'count', 'camera', 'created_at', 'updated_at'];
}
